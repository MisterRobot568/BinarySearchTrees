class Node {
    constructor(data) {
        this.data = data;
        this.left = null;
        this.right = null;
        // we set right and left to null initially because we don't set them
        // when we create the node
    }
}

export default Node;
